#!empty
** TODO: Something
class Hello
{
  static Void main() 
  {
	echo("hello world!")
  }
	
  /* This comment may span multiple lines. */
  Int func()
  {
	// FIXME: Nothing
	echo(Str <|no \ or $ escapes need, and
      multi-line works too|>)
	return 4
  }
}